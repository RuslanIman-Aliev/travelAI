/**
 * Mounted under this by `main.ts`, and applied by the docs script before the
 * document is built - otherwise the generated paths would be missing it and no
 * example in the documentation would be copyable.
 */
export const API_PREFIX = 'api/v1';
